{
  "name": "kernelboard",
  "dependencies": {
    "rehype-raw": "^7.0.0",
    "tailwindcss": "^3.4.17"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --include=dev && npm run build"
  }
}
